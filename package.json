{
  "name": "@dictadata/dictadata-client",
  "version": "1.1.8",
  "description": "Vue.js library for shared components and files.",
  "author": "Drew O. Letcher",
  "license": "MIT",
  "main": "index.js",
  "repository": "github:dictadata/dictadata-client",
  "scripts": {
    "debug": "set NODE_ENV=development&& node --inspect-brk test/lib/<some file to test>.js",
    "lint": "eslint lib/**/*.js",
    "lint-fix": "eslint --fix lib/**/*.js"
  },
  "dependencies": {},
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-promise": "^4.2.1"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
